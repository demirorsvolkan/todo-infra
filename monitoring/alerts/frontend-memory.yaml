apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule

metadata:
  name: frontend-memory-alerts
  namespace: monitoring
  labels:
    release: prometheus

spec:
  groups:
    - name: frontend-memory
      rules:

        - alert: FrontendMemoryNotice
          expr: |
            (
              container_memory_working_set_bytes{
                namespace="todo-app",
                container="frontend"
              }
              / on(namespace,pod,container)
              kube_pod_container_resource_limits{
                namespace="todo-app",
                container="frontend",
                resource="memory",
                unit="byte"
              }
            ) * 100 > 65 and
            (
              container_memory_working_set_bytes{
                namespace="todo-app",
                container="frontend"
              }
              / on(namespace,pod,container)
              kube_pod_container_resource_limits{
                namespace="todo-app",
                container="frontend",
                resource="memory",
                unit="byte"
              }
            ) * 100 <= 75
          for: 5m
          labels:
            severity: notice
            component: frontend
            alert_source: todo-app
          annotations:
            summary: "Frontend memory usage above 65%"
            description: "Frontend memory usage exceeded 65% of its memory limit."
            memory_usage: '{{ printf "%.1f" $value }}%'

        - alert: FrontendMemoryWarning
          expr: |
            (
              container_memory_working_set_bytes{
                namespace="todo-app",
                container="frontend"
              }
              / on(namespace,pod,container)
              kube_pod_container_resource_limits{
                namespace="todo-app",
                container="frontend",
                resource="memory",
                unit="byte"
              }
            ) * 100 > 75 and
            (
              container_memory_working_set_bytes{
                namespace="todo-app",
                container="frontend"
              }
              / on(namespace,pod,container)
              kube_pod_container_resource_limits{
                namespace="todo-app",
                container="frontend",
                resource="memory",
                unit="byte"
              }
            ) * 100 <= 85
          for: 5m
          labels:
            severity: warning
            component: frontend
            alert_source: todo-app
          annotations:
            summary: "Frontend memory usage above 75%"
            description: "Frontend memory usage exceeded 75% of its memory limit."
            memory_usage: '{{ printf "%.1f" $value }}%'

        - alert: FrontendMemoryCritical
          expr: |
            (
              container_memory_working_set_bytes{
                namespace="todo-app",
                container="frontend"
              }
              / on(namespace,pod,container)
              kube_pod_container_resource_limits{
                namespace="todo-app",
                container="frontend",
                resource="memory",
                unit="byte"
              }
            ) * 100 > 85 and
            (
              container_memory_working_set_bytes{
                namespace="todo-app",
                container="frontend"
              }
              / on(namespace,pod,container)
              kube_pod_container_resource_limits{
                namespace="todo-app",
                container="frontend",
                resource="memory",
                unit="byte"
              }
            ) * 100 <= 95
          for: 2m
          labels:
            severity: critical
            component: frontend
            alert_source: todo-app
          annotations:
            summary: "Frontend memory usage above 85%"
            description: "Frontend memory usage exceeded 85% of its memory limit."
            memory_usage: '{{ printf "%.1f" $value }}%'

        - alert: FrontendMemoryEmergency
          expr: |
            (
              container_memory_working_set_bytes{
                namespace="todo-app",
                container="frontend"
              }
              / on(namespace,pod,container)
              kube_pod_container_resource_limits{
                namespace="todo-app",
                container="frontend",
                resource="memory",
                unit="byte"
              }
            ) * 100 > 95
          for: 1m
          labels:
            severity: emergency
            component: frontend
            alert_source: todo-app
          annotations:
            summary: "Frontend memory usage above 95%"
            description: "Frontend memory usage exceeded 95% of its memory limit."
            memory_usage: '{{ printf "%.1f" $value }}%'
